import { MatrixRainWebGPU } from '@lib';

// The hero's living backdrop — renders the PUBLIC component (dogfoods the real
// public API). Fills its positioned parent; the page provides the container.
export function HeroRain() {
  return <MatrixRainWebGPU />;
}
